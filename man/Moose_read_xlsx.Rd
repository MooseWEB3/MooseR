% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_read_csv.R
\name{Moose_read_xlsx}
\alias{Moose_read_xlsx}
\title{Import XLSX files}
\usage{
Moose_read_xlsx(
  path,
  file_name = NULL,
  prefix = "imported_data",
  envir = parent.frame(),
  recursive = FALSE,
  overwrite = FALSE,
  clean_names = TRUE,
  sheet = 1,
  ...
)
}
\arguments{
\item{path}{A directory to import, or a single file path.}

\item{file_name}{Optional file name within \code{path}. When supplied, only
that file is returned.}

\item{prefix}{Prefix used for objects created during directory imports.}

\item{envir}{Environment receiving objects during directory imports.}

\item{recursive}{Logical. Search subdirectories during directory imports.}

\item{overwrite}{Logical. Allow existing object names in \code{envir} to be
replaced. The default is \code{FALSE}.}

\item{clean_names}{Logical. Clean imported column names using MooseR's
existing CSV-name rules.}

\item{sheet}{Sheet name or position passed to \code{readxl::read_excel()}.
The default imports the first worksheet from every workbook.}

\item{...}{Additional arguments passed to \code{readxl::read_excel()}.}
}
\value{
For a single file, the imported worksheet. For a directory, an invisible
named list containing all imported worksheets.
}
\description{
Imports every XLSX workbook in a directory, assigns each selected worksheet
as \code{imported_data_001}, \code{imported_data_002}, and so on, and reports the
mapping. Files are processed in deterministic file-name order. Passing a
workbook directly as \code{path}, or supplying \code{file_name}, returns one imported
worksheet without assigning it.
}
\examples{
\dontrun{
Moose_read_xlsx("C:/data")
Moose_read_xlsx("C:/data", sheet = "Data")
}
}
