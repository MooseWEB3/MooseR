% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_known_name_only.R
\name{Moose_name_flag2}
\alias{Moose_name_flag2}
\title{Flag only names supplied in row-aligned columns}
\usage{
Moose_name_flag2(
  text,
  data,
  name_columns = c("first_name", "last_name"),
  max_distance = 1L,
  min_chars = 5L
)
}
\arguments{
\item{text}{A character or factor vector containing text to mask.}

\item{data}{A data frame with the same number of rows as \code{text}.}

\item{name_columns}{Character vector naming row-aligned known-name columns.}

\item{max_distance}{Maximum Levenshtein edit distance, from 0 to 3.}

\item{min_chars}{Minimum normalized length of a known name eligible for
similar matching. Exact matching is not restricted by this value.}
}
\value{
An integer vector containing \code{1L} for matching rows and \code{0L} for
non-matching, missing, or blank rows.
}
\description{
Returns \code{1L} only when text contains an exact or similar spelling of a name
supplied by \code{name_columns} in the same row of \code{data}. It uses the same
restricted matching rules as \code{\link[=Moose_mask_person_names2]{Moose_mask_person_names2()}} and does not run
general personal-name detection.
}
\examples{
records <- data.frame(
  comments = c("Jonathon Smyth called.", "Peter Brown called."),
  first_name = c("Jonathan", "Alice"),
  last_name = c("Smith", "Martin")
)
Moose_name_flag2(
  records$comments,
  data = records,
  name_columns = c("first_name", "last_name")
)
}
