% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_boost_data.R
\name{Moose_boost_data}
\alias{Moose_boost_data}
\title{Automatically convert date-like columns in a data set}
\usage{
Moose_boost_data(
  data,
  day_first = FALSE,
  numeric_origin = c("auto", "excel", "unix", "r"),
  tz = "UTC",
  min_success = 0.8,
  verbose = TRUE,
  two_digit_year_cutoff = 68L
)
}
\arguments{
\item{data}{A data frame or data-frame subclass.}

\item{day_first}{Logical fallback for ambiguous numeric dates. MooseR first
examines other values in the same column to infer month/day/year or
day/month/year. If the evidence is tied or absent, \code{TRUE} prefers
day/month/year and \code{FALSE} prefers month/day/year.}

\item{numeric_origin}{Character. One of \code{"auto"}, \code{"excel"}, \code{"unix"}, or
\code{"r"}. Passed to \code{\link[=Moose_todate]{Moose_todate()}} and \code{\link[=Moose_todatetime]{Moose_todatetime()}}.}

\item{tz}{Time zone used when parsing date-time columns.}

\item{min_success}{Numeric value between 0 and 1. At least this proportion
of non-missing values must look like dates and convert successfully before
a column is changed.}

\item{verbose}{Logical. If \code{TRUE}, report which columns were converted.}

\item{two_digit_year_cutoff}{Whole number from 0 to 99. Passed to
\code{\link[=Moose_todate]{Moose_todate()}} and
\code{\link[=Moose_todatetime]{Moose_todatetime()}}.}
}
\value{
A copy of \code{data} with confidently detected date columns converted
to \code{Date} and date-time columns converted to \code{POSIXct}.
}
\description{
Inspects each column of a data frame and converts columns that strongly
resemble dates or date-times. Existing \code{Date} and \code{POSIXt} columns are left
unchanged. Character and factor columns are recognized from their values;
numeric columns also require a date-related column name to reduce false
positives.
}
\examples{
raw_data <- data.frame(
  visit_date = c("2024-01-05", "2024/01/06"),
  created_at = c("2024-01-05 13:30", "2024-01-06 08:05"),
  note = c("First visit", "Follow-up")
)

boosted <- Moose_boost_data(raw_data)
class(boosted$visit_date)
class(boosted$created_at)
}
