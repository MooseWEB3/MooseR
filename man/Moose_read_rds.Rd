% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_read_csv.R
\name{Moose_read_rds}
\alias{Moose_read_rds}
\title{Import RDS files}
\usage{
Moose_read_rds(
  path,
  file_name = NULL,
  prefix = "imported_data",
  envir = parent.frame(),
  recursive = FALSE,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{path}{A directory to import, or a single file path.}

\item{file_name}{Optional file name within \code{path}. When supplied, only
that file is returned.}

\item{prefix}{Prefix used for objects created during directory imports.}

\item{envir}{Environment receiving objects during directory imports.}

\item{recursive}{Logical. Search subdirectories during directory imports.}

\item{overwrite}{Logical. Allow existing object names in \code{envir} to be
replaced. The default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[base:readRDS]{base::readRDS()}}.}
}
\value{
For a single file, the restored R object. For a directory, an invisible named
list containing all restored objects.
}
\description{
Imports every RDS file in a directory, assigns the restored objects as
\code{imported_data_001}, \code{imported_data_002}, and so on, and reports the mapping.
Files are processed in deterministic file-name order. Passing an RDS file
directly as \code{path}, or supplying \code{file_name}, returns one restored object
without assigning it.
}
\examples{
rds_dir <- tempfile()
dir.create(rds_dir)
saveRDS(data.frame(value = 1), file.path(rds_dir, "a.rds"))
imported <- new.env(parent = emptyenv())
Moose_read_rds(rds_dir, envir = imported)
imported$imported_data_001
unlink(rds_dir, recursive = TRUE)
}
