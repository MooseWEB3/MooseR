% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_rds.R
\name{Moose_SRds}
\alias{Moose_SRds}
\alias{Moose_LRds}
\title{Save and load R objects in RDS format}
\usage{
Moose_SRds(data, file = NULL, compress = TRUE, overwrite = TRUE)

Moose_LRds(file)
}
\arguments{
\item{data}{Any R object to save.}

\item{file}{A single file path. In \code{Moose_SRds()}, the object name is used
when \code{file} is \code{NULL}.}

\item{compress}{Logical or one of \code{"gzip"}, \code{"bzip2"}, or \code{"xz"}. Controls
compression passed to \code{\link[base:readRDS]{base::saveRDS()}}.}

\item{overwrite}{Logical. If \code{FALSE}, stop rather than replace an existing
file.}
}
\value{
\code{Moose_SRds()} invisibly returns the normalized path of the saved
file. \code{Moose_LRds()} returns the restored R object.
}
\description{
\code{Moose_SRds()} saves one R object to an RDS file. If \code{file} is omitted, the
object name is used as the file name. \code{Moose_LRds()} restores the object
without changing its class or attributes. Both functions add the \code{.rds}
extension when it is omitted.
}
\examples{
rds_file <- tempfile(fileext = ".rds")
saved_path <- Moose_SRds(iris, rds_file)
restored <- Moose_LRds(saved_path)
identical(iris, restored)
unlink(saved_path)
}
