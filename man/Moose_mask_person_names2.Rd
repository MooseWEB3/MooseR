% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Moose_known_name_only.R
\name{Moose_mask_person_names2}
\alias{Moose_mask_person_names2}
\title{Mask only names supplied in row-aligned columns}
\usage{
Moose_mask_person_names2(
  text,
  data,
  name_columns = c("first_name", "last_name"),
  replacement = "[NAME]",
  max_distance = 1L,
  min_chars = 5L
)
}
\arguments{
\item{text}{A character or factor vector containing text to mask.}

\item{data}{A data frame with the same number of rows as \code{text}.}

\item{name_columns}{Character vector naming row-aligned known-name columns.}

\item{replacement}{Replacement text used for matched names.}

\item{max_distance}{Maximum Levenshtein edit distance, from 0 to 3.}

\item{min_chars}{Minimum normalized length of a known name eligible for
similar matching. Exact matching is not restricted by this value.}
}
\value{
A character vector with the same length as \code{text}.
}
\description{
Masks exact or similar spellings of names supplied by \code{name_columns} in the
same row of \code{data}. Unlike \code{\link[=Moose_mask_person_names]{Moose_mask_person_names()}}, this function does
not run spaCy, the general name regex engine, or non-person whitelists. Names
not represented by the same row's known-name columns are left unchanged.
}
\details{
Exact matching is case-insensitive and supports individual fields,
\code{First Last}, and \code{Last, First} forms. Similar matching uses Levenshtein edit
distance on individual name tokens after removing accents and punctuation.
By default, one edit is allowed only for known names containing at least
five letters. Set \code{max_distance = 0L} to disable spelling-error tolerance.
}
\examples{
records <- data.frame(
  comments = c("Jonathon Smyth called.", "Peter Brown called."),
  first_name = c("Jonathan", "Alice"),
  last_name = c("Smith", "Martin")
)
Moose_mask_person_names2(
  records$comments,
  data = records,
  name_columns = c("first_name", "last_name")
)
}
